# Project State: Nicola Capasso Portfolio

Last updated: 2026-04-27

## What was done last session
- Frontend audit completo del sito Nicola.
- Fix applicati e pushati (commit `3e1d803`):
  - `fetchPriority` → `fetchpriority` (warning React)
  - `localStorage` wrappato in try/catch (4 file)
  - Marquee rispetta `prefers-reduced-motion`
  - Rimosso dead code toast/toaster/use-toast
  - Rimosso fallback `process.env` inutile in Contact
  - Rimosso `document.title` duplicato in Legal
  - NotFound usa `PictureImg` per WebP/AVIF
  - Rimosso `sizes` ridondante in PictureImg
  - Rimosso throw in `site.ts` che rompeva build senza env
- Build OK, typecheck OK, lint OK, tests 8/8 OK.

## Open items / next steps
- M6 (Lightbox PictureImg) e M7 (Portfolio ref callback) lasciati per futuro — non critici.

## Current file status
- `public/images/` has 58 JPG + 58 WebP.
- Code already falls back from WebP to JPG in `<picture>` sources, so both formats are fine.
- Dist output includes both.

## Open items / next steps
- Review whether original JPGs should be removed from repo to save space (client may need them as fallbacks for older browsers).
- Next planned task: review site content/layout with client feedback, or add new sections if needed.

## Deployment
- Target: Netlify (static build from repo root).
- Domain: nicolacapassophoto.com
- Build command: `npm run build`
- Publish dir: `dist`
